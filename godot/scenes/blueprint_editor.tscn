[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/blueprint_editor.gd" id="1_ed"]

; Standalone editor scene. Run via "Play this scene" (F6) from the Godot
; editor, or set as main scene temporarily. All layout is constructed in
; script so the .tscn stays trivial and survives schema changes.

[node name="BlueprintEditor" type="Control"]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
script = ExtResource("1_ed")
